#!/bin/sh
############################################################################
#
#  Makefile to compile and link a C++ application to 
#  Cantera.
#
#############################################################################

# addition to suffixes
.SUFFIXES : .d

# the name of the executable program to be created
PROG_NAME = airNASA9_pairs

# the object files to be linked together. List those generated from Fortran
# and from C/C++ separately
OBJS = airNASA9_pairs.o

# This link is to Cantera's install space
CT_INCROOT=@ct_incroot@

# Bring in the Cantera includes through the .mak file
include $(CT_INCROOT)/cantera/Cantera.mak

# Define the include directories
INCLUDE_DIRS =  $(CANTERA_TOTAL_INCLUDES)

# the C++ compiler
CXX = @CXX@

# C++ compile flags
CXX_FLAGS = @CXXFLAGS@ $(INCLUDE_DIRS)

# Ending C++ linking libraries
LCXX_END_LIBS = @LCXX_END_LIBS@

# flags passed to the C++ compiler/linker for the linking step
LCXX_FLAGS = @CXXFLAGS@

# Program Name
PROGRAM = $(PROG_NAME)$(EXE_EXT)

all: $(PROGRAM) airNASA9_pairs_300 .depends

# How to compile C++ source files to object files
.cpp.o:
	$(CXX) -c $< $(CXX_FLAGS)

# How to compile the dependency file
.cpp.d:
	@CXX_DEPENDS@ $(CXX_FLAGS) $*.cpp > $*.d

# List of dependency files to be created
DEPENDS=$(OBJS:.o=.d)  airNASA9_pairs_300.d

$(DEPENDS): Makefile

$(OBJS): Makefile

$(PROGRAM): $(OBJS) $(CANTERA_CORE_LIBS_DEP)
	$(CXX) -o $(PROGRAM) $(OBJS) $(LCXX_FLAGS) \
                  $(CANTERA_TOTAL_LIBS)\
                  $(LCXX_END_LIBS)

airNASA9_pairs_300: airNASA9_pairs_300.o $(CANTERA_CORE_LIBS_DEP)
	$(CXX) -o airNASA9_pairs_300 airNASA9_pairs_300.o $(LCXX_FLAGS)  \
                  $(CANTERA_TOTAL_LIBS) \
                  $(LCXX_END_LIBS)

# depends target -> forces recalculation of dependencies
depends:
	$(RM) *.d .depends
	@MAKE@ .depends

.depends: $(DEPENDS)
	cat *.d > .depends

# Do the test -> For the windows vc++ environment, we have to skip checking on
#                whether the program is uptodate, because we don't utilize make
#                in that environment to build programs.
test:
	$(MAKE) all
	./runtest

rebaseline:
	cp output.txt output_blessed.txt

clean:
	$(RM) $(OBJS) *.o $(PROGRAM) $(DEPENDS) .depends airNASA9_pairs_300
	(if test -d SunWS_cache ; then \
           $(RM) -rf SunWS_cache ; \
         fi )

#############################################################################
#          DEPENDS RULES
##############################################################################

ifeq ($(wildcard .depends), .depends)
include .depends
endif

